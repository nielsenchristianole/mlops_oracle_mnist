model:
  model_name: mobilenetv3_small_100
  pretrained: true
  num_classes: 10
data:
  processed_dir: data/processed
  raw_dir: data/raw
train:
  optimizer:
    _target_: torch.optim.Adam
    lr: 0.001
    betas:
    - 0.9
    - 0.999
    eps: 1.0e-08
    weight_decay: 0
  scheduler:
    _target_: src.oracle_mnist.scheduler.sarphiv_scheduler.temp
    test: hej
  batch_size: 32
  epochs: 10
misc:
  seed: 42
